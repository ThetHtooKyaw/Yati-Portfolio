{
  "version": 2,
  "builds": [
    {
      "src": "**/*.dart",
      "use": "@vercel/static-build",
      "config": {
        "installCommand": "if cd flutter; then git pull && cd .. ; else git clone https://github.com/flutter/flutter.git; fi && flutter/bin/flutter clean && flutter/bin/flutter config --enable-web && flutter/bin/flutter pub get",
        "buildCommand": "flutter/bin/flutter build web --release",
        "outputDirectory": "build/web"
      }
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/build/web/index.html" }
  ]
}
